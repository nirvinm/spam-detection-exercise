export const SHINGLE_SIZE = 4; // not used currently since we are splitting the email text by space
export const LSH_BUCKET_SIZE = 50; // configures number of buckets in LSH
export const HASH_FUNCTIONS_COUNT = 100; // configures number of hash functions to generate in LSH
export const MIN_SIMILARITY = 0.8; // minimum similarity score for an email to be considered related to another email
